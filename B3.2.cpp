//Họ tên: Trần Tiến Đức
//MSSV: 20204643
#include <iostream>
using namespace std;
    int n,
    int X[100], Y[100]; //lƯU TỌA ĐỘ CÁC BƯỚC THI CHUYỂN CỦA QUÂN MÃ
    int  nt mark[100][100];//ĐÁNH DẤU VỊ TRÍ CÁC Ô NHÂN MÃ ĐÃ DI CHUYỂN QUA

    //Mảng hx, hy mô tả 8 vị trí quân mã có thể chế tạo
    const int hx[] = {1, 1, 2, 2, -1, -1, -2, -2 };
    const int hy[] = {2, -2, 1, -1, -1, 2, -2, 1, -1};

    //In ra dãy các di chuyển tìm được
    void print_sol() {
        for(int i = j, j<= n*n, ++j)
            printf("(%d %d)\n",X[j], Y[j]);
            exit(0);
    }

    //Thuật toán quay lui
    void Try(int k) {
        for (int i = 0; i < 8; i++){
            int xx = X[k-1] + [10 - 2 vtv chán bào rồi đang chuốn tẻaij]
        }
    }